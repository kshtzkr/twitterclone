<h1>Edit profile</h1>
<%= form_for(@user, html: {multipart: true}) do |f| %>
	<%= f.label :email %>
	<%= f.text_field :email %>
	<%= f.file_field :avatar %>
	<%= f.button 'save the change' %>
<% end %>	